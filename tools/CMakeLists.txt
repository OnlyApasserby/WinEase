# ============================================================================
#  构建期工具（不属于产品的一部分，但属于"能不能交付"的一部分）
#
#  tools/sfx —— 自解压安装器 SFX 启动器（winease-setup.exe）
#      它是"用户拿到的那一个 exe"的外壳：静态链接 CRT、不依赖 Qt，
#      本质上是"Win32 + cabinet.dll"的小程序。打包编排见 cmake/WinEaseDist.cmake。
# ============================================================================

add_subdirectory(sfx)
