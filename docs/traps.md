# WinEase 实现踩坑记录

> 本文件于 2026-09-14 从 [`ROADMAP.md`](./ROADMAP.md) 拆出，**内容与编号一字未改**（拆出只是为了把路线图缩短到"任务与决策"该有的长度）。

**编号是全工程共享的**：`docs/ROADMAP.md` 的变更记录、各插件与被测源码注释里写的"踩坑 #NN"都指向本文件的编号 —— 不要重新编号，也不要把编号当成"只属于某一批"的东西。
**写代码前先在这儿对照一遍**：这里的每一条都是真实踩过的，不是理论风险。

| # | 现象 | 根因 | 处理 |
|---|---|---|---|
| 1 | `DWMWA_EXTENDED_FRAME_BOUNDS` 返回的视觉边界**比 GetWindowRect 还大**（758 vs 520） | DWM 的该属性**始终返回物理像素**，而 DPI 不感知进程里 `GetWindowRect` 返回**被虚拟化**的像素，两者相差一个缩放倍数 | 自检程序显式声明 per-monitor DPI aware v2（与 WinEase.exe 一致）；`WindowUtils` 内部另做自洽换算作为安全网。**新增非 GUI 辅助进程（如 WinEaseHelper）时必须同样声明 DPI 感知** |
| 2 | `std::vector<wchar_t>` 插入 `QString` 内容编译失败（`construct_at` 无匹配重载） | `QString` 的 `value_type` 是 `char16_t`，与 `wchar_t` 不是同一类型，无隐式转换 | 逐字符 `static_cast<wchar_t>(ch.unicode())`，并在代码中注明原因 |
| 3 | `toLogical`/`toPhysical` 往返出现 ±1 像素偏差（640@150% → 427 → 641） | 取整的固有损耗，不是缺陷 | 在头文件中明确标注"最多 1 像素取整损耗，不要依赖精确往返"；自检容差设为 1 像素 |
| 4 | 日志消息处理器内部再打日志会死锁 | 自定义 handler 与日志写入共用同一把非递归互斥锁 | 必须**先解锁再输出日志**（已在 `Logging::install()` 处理） |
| 5 | `qt_add_plugin` 报 `Cannot find source file: .../SOURCES` | `qt_add_plugin` **不接受 `SOURCES` 关键字** | 源文件列表直接展开（已在 `WinEasePlugin.cmake` 处理） |
| 6 | `find_package(Qt6 COMPONENTS Multimedia)` 后 `Qt6::MultimediaWidgets` 不存在 | 未请求该组件时不会生成对应 CMake 目标 | 同时请求 `Multimedia MultimediaWidgets`（已在根 CMakeLists 处理） |
| 7 | 插件编译报 moc `Undefined interface` | `Q_DECLARE_INTERFACE` 缺失 | 必须写在 `IFeaturePlugin.h` 末尾 |
| 8 | 插件编译报 `connect` 访问不明确 / moc `qt_metacast` 转换歧义 | 插件类重复继承 `QObject`（`IFeaturePlugin` 已继承），产生二义基类 | 插件**只继承 `IFeaturePlugin`** |
| 9 | **`QString::toStdWString()` 把 12 个 UTF-16 单元变成 15 个，转回 QString 得到空串；进程退出时访问违例** | **构建配置与 Qt 不一致**：`D:\Qt` 是 release-only 安装（无 `Qt6Cored.dll`），而工程用 `--config Debug` 构建。MSVC 下 Debug 与 Release 的 **STL 类型布局与 CRT 堆不兼容**，凡是"Qt 返回/接收 STL 类型"的接口（`std::wstring`/`std::string`/`std::vector`）都会内存损坏。症状极其隐蔽：返回值看似正常、退出时才崩 | **必须用 `RelWithDebInfo` 或 `Release` 构建**；已在根 CMakeLists 加入"Qt 无调试库"警告；自检中新增"跨模块 STL 互操作"诊断项（Debug 下报警告，Release 下通过）。实测：Release 103/103 通过 exit 0，Debug 同代码警告 + 退出违例 |
| 10 | 调用 `Windows.Foundation.Metadata.ApiInformation::IsTypePresent` 直接崩溃（`winrt/base.h:2948 assert(value)`） | 该 API **面向有"包标识"(package identity) 的应用**；未打包桌面程序调用它命中 C++/WinRT 内部断言，而不是优雅返回错误 | 改用 `RoGetActivationFactory` 探测运行时类（`probeWinRtClass`）。注意请求 `IID_IUnknown` 而非 `IActivationFactory`——静态类（如 `PdfDocument`）的工厂实现的是自己的静态接口，请求 `IActivationFactory` 会对可用类型误报 `E_NOINTERFACE` |
| 11 | 网络吞吐原计划用 `GetIfTable2` + `MIB_IF_TABLE2`，编译报"未声明的标识符" | `netioapi.h` 位于 SDK 的 `shared` 目录，且其在用户态分支依赖先定义 `__IPHLPAPI_H__`；排查成本高于收益 | 改用 PDH 计数器 `\Network Interface(*)\Bytes Received/sec`：直接给出速率、复用已有 PDH 封装、与 CPU/GPU 采样一致。代价是累计值只能由速率积分 |
| 12 | `SHGetImageList` 编译报 `IImageList` 未定义 / 函数不接受 2 个参数 | `IImageList` 在 `commoncontrols.h` 中声明，且该头需要 `commctrl.h` 先定义 `IMAGELISTDRAWPARAMS`/`IMAGEINFO` | 依次包含 `commctrl.h` → `commoncontrols.h` |
| 13 | 亮度/对比度函数找不到（`MC_CAPS_*` 未声明） | 函数在 **`highlevelmonitorconfigurationapi.h`**，而 `lowlevelmonitorconfigurationapi.h` 提供的是底层 VCP 原始读写 | 包含 `highlevelmonitorconfigurationapi.h` + `physicalmonitorenumerationapi.h` |
| 14 | 自检中 COM 相关测试在通过后又失败（"当前线程尚未初始化 COM"） | `ComApartment::initialize()` 返回 RAII 守卫，在函数内声明会**随函数返回析构**并 `CoUninitialize` | 守卫必须存活到进程结束（这是该类的契约，已在头文件写明）；从自检里学到：**契约必须写进文档，否则一定被误用** |
| 15 | 跨 DLL 投递自定义 `QEvent` 会失效：主程序创建的事件，插件永远认不出来 | **`QEvent::registerEventType()` 在每个模块各调用一次会返回不同的 type id**（它是进程内递增分配，而静态库被主程序与每个插件各链接一份，各有一份静态变量）。用 `QEvent::User + N` 这种固定偏移也会和其它模块的偏移冲突 | 排队投递改用 **lambda 形式的 `QMetaObject::invokeMethod(obj, lambda, Qt::QueuedConnection)`**：不需要统一事件类型 id、不需要 `qRegisterMetaType`，而且接收者析构时 Qt 会自动撤销未执行的调用（不会悬空）。**凡是"静态库 + 多模块"的结构，都不要用 `registerEventType()` 跨模块传事件** |
| 16 | 低层钩子的回调"装上了却不触发" | `PostsThreadMessage`/钩子回调都要求目标线程**已建立消息队列**。对还没有消息队列的线程调用 `PostThreadMessage` 返回 `ERROR_INVALID_THREAD_ID`；低层钩子的回调也依赖安装线程的消息循环才能被投递 | 钩子线程 `run()` 里先 `PeekMessage(&msg, nullptr, WM_USER, WM_USER, PM_NOREMOVE)` 强制创建消息队列，再用 `GetMessage` 循环；主线程等钩子线程安装完成用 `QSemaphore` 同步（不能靠 `msleep` 猜） |
| 17 | 主程序接到 `CloseMainWindow()`（WM_CLOSE）不退出 | 托盘常驻应用的标准行为——关窗即最小化到托盘，退出走托盘菜单 | 属于预期行为，不是缺陷。副作用：自动化脚本无法用"关闭窗口"来验证优雅退出路径（`shutdown()` 里的钩子统计日志需手工经托盘退出验证） |
| 18 | 自检里"关闭点击穿透后该点应被悬浮层遮挡"一直失败：`WindowFromPoint` 始终返回下层窗口 | 半透明悬浮层是**分层窗口（`WS_EX_LAYERED`）**，Windows 对它的命中测试**逐像素按 alpha 判定**：alpha == 0 的区域无论有没有 `WS_EX_TRANSPARENT` 都不会被命中。自检里的悬浮层为了不干扰用户，什么都不画（全透明），于是"没被命中"是必然的——**测试假通过** | 教训有两条：<br>① 验证点击穿透时，悬浮层**必须画出不透明内容**，否则测的是"透明"而不是"穿透"（自检已改为在探针点画一块不透明色块，并加反向对照：关闭穿透后必须命中悬浮层本身）；<br>② 做**可交互**悬浮层（`setClickThrough(false)`）时，必须在该区域真正绘制不透明内容才收得到点击；只画边框的"焦点高亮"式悬浮层中间永远点不到 |
| 20 | 自检里用 `QStyle::objectName()` 判断"样式是 Fusion" → 拿到**空字符串**，于是"切换前后样式未变"退化成"空串 == 空串"，**永远通过**；换成比较 `QApplication::style()` 的类名后又发现**仍然是 QStyleSheetStyle**，同样永远"没变"（连续两处假通过） | ① `QStyleFactory::create("Fusion")` 手工创建的样式实例 **`objectName()` 就是空的**（只有平台样式与样式插件会设置它）；② **一旦设置全局样式表，`QApplication::style()` 返回的是 Qt 自动包上的代理 `QStyleSheetStyle`**，真实样式藏在它内部 | 用 `metaObject()->className()` 而不是 `objectName()`；剥代理**不能用** `qobject_cast`/`dynamic_cast<QProxyStyle*>` —— 实测 Qt 6.8.4 的代理是 `class QStyleSheetStyle : public QWindowsStyle`（`QtWidgets/private/qstylesheetstyle_p.h`），根本不是 QProxyStyle。唯一可行且只依赖公开 API 的办法是**临时清空应用样式表**：Qt 收到空样式表会 `setStyle(proxy->base); proxy->deref();`，此刻 `QApplication::style()` 就是真实底层样式，读完立刻把原样式表装回，并**加一条断言确认装回成功**（否则后续基于样式表的断言全在错误样式上做） |
| 21 | 取像素断言总是失败：明明窗口底色是 `#000000`，`(4,4)` 却读出别的颜色 | `QWidget::grab()` 返回的位图是**设备像素**（本机 DPR 1.5，360×260 逻辑 = 540×390 设备），而控件 `geometry()`/`rect()` 是**逻辑像素**，两者直接混用就会取错位置；`QImage::devicePixelRatio()` 还可能保持 1.0 而**不反映真实缩放** | 取像素前先按 `frame.devicePixelRatio()`（不合法则用 1.0）把逻辑矩形换算成设备矩形再统计；断言失败时把两个坐标系的尺寸一起打出来，便于一眼看出是不是差了一个 DPR |
| 22 | 样式表的静态审查"通过"了，可实际上样式表里仍然有会跟随系统的写法 | 极简 QSS 解析器**没剥注释**，而注释里恰好写着 `palette(...)`、`#000000` 之类的说明文字，被当成真实声明喂给了断言（**假通过**） | 静态审查一律**先剥注释再检查**；同时把"扫描了多少条声明"一起打印出来作为**覆盖度自证**（本项目实际扫到 303 条声明 / 70 条 background / 27 处图标颜色），否则"零命中"既可能是真干净，也可能是压根没扫到 |
| 23 | 在进程内把系统主题改成浅色后，"应用是否跟随系统"这件事**根本验证不了** | `QWindowsTheme` **只在启动时**读一次色彩方案（`HKCU\...\Themes\Personalize\AppsUseLightTheme`）并缓存，运行期改写注册表 + 广播 `WM_SETTINGCHANGE "ImmersiveColorSet"` 并不会让它重新读取（实测本机 Qt 6.8.4 完全不重读） | 把"系统浅色"当成**环境前置条件**而不是在进程内模拟：临时改注册表 → **另起一个子进程**从零启动 → 子进程先断言自己确实处于浅色（`styleHints()->colorScheme() == Light`，**前置条件断言**，不成立就返回专用退出码 2，避免"场景没搭成却报通过"）→ 再断言黑底白字 → 父进程等子进程退出码并恢复注册表（恢复后回读校验）。这也更贴近用户真实用法："系统是浅色，然后打开 WinEase" |
| 24 | 提权助手用 `QLocalServer` 监听后，普通权限进程连接管道一律"拒绝访问"——客户端根本连不上，但管道明明存在 | **Windows 强制完整性控制（MIC）**：提权（High IL）进程创建的命名管道默认带 High 完整性标签，普通（Medium IL）进程被**禁止写入**；而 `QLocalServer`（Qt 6.8）没有暴露设置 DACL/完整性标签的任何入口（Qt 6.9 才有 `SocketOptions`） | helper 端改用 **Win32 原生管道服务**：`CreateNamedPipeW` + `ConvertStringSecurityDescriptorToSecurityDescriptorW` 显式指定 `D:(A;;GRGW;;;WD)S:(ML;;NW;;;ME)`（Everyone 可读写 + Medium 完整性标签 No-Write-Up）。接受循环在专用线程，每个连接再开独立线程，**先创建下一个监听实例再处理客户端**（否则并发连接会撞 ERROR_PIPE_BUSY）。客户端 `QLocalSocket` 无需改动 |
| 25 | 助手拒绝合法调用方："不在同一程序目录"，可两边明明都在 `build\bin` | `QueryFullProcessImageNameW` 返回**反斜杠**路径，`QFileInfo::absolutePath()` 返回**正斜杠**路径，`QString::compare` 逐字符比较必然不等 | 比较前统一 `QDir::cleanPath(QDir::fromNativeSeparators(...)).toLower()` 归一化两侧目录 |
| 26 | hosts 字节级还原后与原始内容差 3 字节（3736 → 3733） | 原文件带 **UTF-8 BOM**（EF BB BF），经 `QString` 进 JSON 传输时被 JSON 解析器剥掉开头 U+FEFF | `writeHosts` 的 replace 模式增加 **`contentBase64`** 字节级通道（base64 往返无损）；测试/运维还原一律走 base64 |
| 27 | 助手第一次写 hosts 成功、紧接着的恢复写入报错误码 5（拒绝访问） | 修改 hosts 会触发安全软件的**短暂锁定**（防篡改监控），间隔几十毫秒内的第二次写入被拒 | 写入失败且错误码为 5 时重试（8 次 × 250ms）；验收自检还提供 `--restore-hosts <备份>` 运维模式 |
| 28 | 浮层与目标显示器尺寸差 1 像素（2560 → 2561） | 非整数缩放（150%）下取整损耗：2560/1.5 = 1706.67 → 逻辑 1707 → 回算 2560.5 → 2561。Qt 窗口几何是**整数逻辑像素**，物理目标 2560 在此缩放下不可能精确命中 | 这不是缺陷，但**方向很重要**：实测取向恒为"不小于目标"，因此覆盖类用途不会在边缘漏缝。已在 `OverlayWindow::setOverlayGeometry()` 写明"宁大勿小"策略；自检断言改为「容差 ≤1 像素 **且** 必须完全包含目标矩形」——把"无缝隙"而不是"相等"作为真正的验收点 |
| 29 | 想在 `Guard::invoke()` 里直接写 `__try` —— 编译报 **C2712**：`cannot use __try in functions that require object unwinding`；反过来若 `__except` 什么都接，C++ 异常**会被误判成 SEH（"访问违例"）**且展开链被截断 | ① MSVC 不允许 `__try` 出现在**需要栈展开**的函数里（函数内有非 POD 局部对象 / 有 try 块）；② `__except` 是"异常码先到先得"，`0xE06D7363` 就是 C++ 异常（`"msc"` 的 ASCII 拼写），必须让它继续往上走 | 把 `__try` 关进**只用 POD 局部变量**的 `callSehProtected(fn, ctx, codeOut)`（实现固定在 `PluginGuard.cpp`，头文件注明"不要往这个函数里加 QString/std::string 局部变量，也不要挪文件"），其余代码一律经 `Guard::invoke()` 间接使用；`__except` 过滤器显式**放行 C++ 异常码 `0xE06D7363` 与栈溢出 `0xC00000FD`**（后者在栈已耗尽时无法安全恢复，只能交系统处理） |
| 30 | 崩溃插件若被正常析构 / `unload()`，程序在退出时"再崩一次"；更有隐蔽性的是：**隔离做了一半**——管理器不再调用插件，但界面统计栏、托盘菜单、快捷键设置对话框、悬浮层兜底回收仍在直接调 `plugin->id()/name()/isEnabled()/icon()` | ① SEH 恢复的本质接近 `longjmp`：**出错帧内已构造的 C++ 对象不会被析构**，对象可能处于"半损坏"状态，跑析构函数 = 二次崩溃；② "不再调用插件"是一条**全局不变量**，只要还有任意一条 UI 路径直连插件对象，隔离就不成立 | 隔离 = **既不调用也不释放**：不调 `shutdown()`、不 `delete` 静态插件、不 unload 插件 DLL（代价是资源泄漏到进程退出，可接受）；并把所有触达插件对象的路径统一改为**缓存读**（`PluginManager::idOf/nameOf/categoryOf/isPluginEnabled/pluginFailureReason`）——加载期元信息探针先把 getter 读一遍缓存起来，正是为了让这条不变量可行。验收要点：断言"插件调用计数在隔离后不再增长"，并**逐条清查 `plugin->` 直连点** |
| 31 | 自检稳定报 2 项失败（"加载错误里给出了原因"），直接读代码看**实现明明是对的** | **陈旧二进制**：源码里的修复（把崩溃原因追加进 `loadErrors()`）从未被编译进 `plugin_smoke.exe` —— 该次构建输出被重定向到 `build_p05c.log`，而**这个日志文件根本不存在**（说明那条"带我重跑一次"的命令实际没执行过），于是复现的仍是旧行为 | 复现失败前**先把构建与测试写成一条命令并确认产物时间戳/日志文件真的产生**；本次重新 `cmake --build` 时日志明确出现 `PluginManager.cpp` 被重新编译 → 重跑即 **36/36 exit 0**。教训：报告失败时除了贴失败项，还要贴"**这次构建到底编译了哪些文件**" |
| 32 | 分屏插件的「居中」每按一次，窗口就小一圈（实测 150% DPI 下每次缩约 18×9 px）| `centerWindowOnMonitor()` 用 `visualWindowRect()` 量出**视觉**宽高，却把它当成**窗口矩形**交给了 `moveWindow()`；两者相差不可见投影边框（左/右各约 7px、下边也有），窗口矩形比视觉边界大，于是"把视觉尺寸当窗口尺寸"= 每次缩掉一个边框的量 | 语义要分清：**"看起来居中"要用视觉边界，`moveWindow()` 收的是窗口矩形** → 经 `windowRectForVisualRect()` 换算后再移动。自检也相应改为断言**视觉矩形**（拿窗口矩形断言会把"边框缩了一圈"当正常）。凡"预览一次就微小变化"的缺陷都靠**连续两次调用 + 尺寸不变**断言抓出来 |
| 33 | 端到端自检里同时出现两种错：① P1-04「置底维持」无论怎么测都"通过"（假通过）；② P1-02/P1-04 部分动作用例报"功能没生效"（假失败）| ① 置底插件用 **`WINEVENT_SKIPOWNPROCESS`** 装钩子 → **装钩子进程自己**引起的 z 序/前台变化被过滤，在测试进程里做实验永远走不到维持逻辑；② 窗口类插件的默认策略是"操作**光标下**的窗口"，而"分屏/置底"这类动作**本身就会让窗口离开光标**（或被压到底层露出别的窗口）→ 后续动作静默地作用到**别的窗口**上（更糟的情况是改到了用户真实窗口，却因为断言只看"插件有没有报错"而假通过）。附带一个时序坑：`WINEVENT_OUTOFCONTEXT` 的回调是**排队投递**的，抬高窗口后立刻做命中测试，可能被迟到的回调又压回去 | ①"另一进程触发"必须做成真的：探针子进程里再开一个**辅助窗口**，先 `SetWindowPos(HWND_TOP)` 抬起目标窗口、再激活辅助窗口产生真实 `EVENT_SYSTEM_FOREGROUND`（子进程抢前台要 `AttachThreadInput` 到当前前台线程，否则前台锁定会拒绝）；②**每个动作之前**重新把光标放回目标窗口，并把这个前置条件**断言出来**（连同"光标在哪/命中了谁/期望是谁"一起打印），前置不成立就报失败而不是继续；③命中测试做成**最多 3 次重试**（每轮先 `processEvents()` 排空积压消息再抬高） |
| 34 | JSON 格式化有两处"看起来对、跑起来全错"：① 格式化后**键顺序被改**（`b` 跑到 `a` 后面）；② 嵌套对象的闭合括号**丢了换行缩进**，输出成 `…{"c": true}]}` | ① `QJsonObject` 内部按 key **排序**存储，`QJsonDocument::toJson()` 往返必然重排 —— "只想排个版"却改了内容；② 空容器判定方向写反了：写的是 `isEmptyContainer(tokens, i-1)`（想问"前一个 token 是不是开括号"），而它实际问的是"下一个 token 是不是闭括号"—— **对任何闭括号都成立**，于是所有非空容器的 `}`/`]` 都走了"空容器"分支 | ① 格式化/压缩改为**自写保序词法扫描**（只切分不重排，拼接即压缩），`QJsonDocument` 退化成"校验器"；② 配对判定改成看**谁和谁配**（前一个 token 恰好是开括号）；③ 这类缺陷靠**整串比对**抓（断言 `format(x) == 预期全文` 与 `minify(format(x)) == x` 往返不变量），只断言"包含某个子串"会全部漏过 |
| 35 | 状态文本 `"完成：哈希摘要 → 3 字符303"`：label 掉到"完成："后面、末尾的"已替换选中"整段消失 | 格式串 `"%1完成：%2 → %3 字符%4"` 只有 4 个占位符，却接了 **5 个 `.arg()`**：多出来的参数被**静默丢弃**，而 `%2` 在"完成："之后，label 自然就错位了。Qt 不会为此报任何错 | 占位符**个数**与 `.arg()` 次数必须一致（写 `"%1%2完成：%3 → %4 字符%5"`）；自检对状态文本要断言**关键片段**（"哈希摘要完成"），只断言"包含哈希摘要"同样漏过 |
| 36 | 端到端自检的两个"环境即真相"陷阱：① P1-01 用"光标移到屏幕角落"当对照条件，结果**时灵时不灵**；② 一次运行里窗口组成批报"前置：光标没落在目标上"（21 项失败），同一份二进制再跑一次只剩 2 项 | ① 平台层 `resolveWindowTarget` 在光标下拿不到可用窗口时会**回退到前台窗口**，而前台窗口往往就是探针自己（子进程一直把自己激活到前台）→ 这个"对照条件"根本没在对照；② 这是**真实桌面**：运行期间有人动鼠标时 `SetCursorPos` 的结果会被抢回去（实测两次运行失败项 21 → 2，且失败项全是"光标位置"类） | ① 对照条件必须**自己挣出来**：改用"另一个真实可操作窗口"（探针子进程的辅助窗口）—— 解析**成功但指向别人**，才是有效对照；② 光标类前置一律**重试到约 0.6 秒**消化短暂干扰，仍失败就如实报"前置不成立"（**绝不静默跳过**），并在 ENV-SETUP 里写明"跑自检时别动鼠标、别操作前台窗口" |
| 37 | P1-08 聚光灯**根本没挖在光标上**：屏幕该亮的地方还是暗的（像素实测 0.57 倍压暗），而用例一开始居然"通过"了 | 两处：① 脏区坐标空间搞错 —— `requestOverlayUpdate()` 收的是**逻辑**矩形，代码却传了用物理像素算出来的圆环外接框 → 去重绘的是**另一块地方**，真正要更新的那一带永远不重绘；② 脏区只算"新位置"，不算"旧位置" → 分层窗口不会自己清理没重绘过的像素，旧的那个洞会一直留在屏幕上。③ 而"假通过"是**前置像素没断言住**：背景板被别的窗口盖住时读到的本来就不是背景板颜色，于是"没被压暗"的阈值判断被"意外的亮"骗过 | ① 脏区一律在**逻辑坐标**下算（`ringBoundsLogical()`，用 `OverlayGeometry` 换算）；② 脏区取"旧 ∪ 新"；③ 前置断言写严：先把"没开聚光灯时该像素就是背景板颜色"断言出来（失败信息里带上 z 序诊断），**再**做 A/B 比对 —— 否则整条像素断言都是空中楼阁 |
| 38 | P1-C 开工第一天就发现：**任何插件都用不了 OverlayKit**（拿不到 `OverlayHost` 的符号），而 `OverlayHost::createOverlay()` 又只能建基类实例 | ① OverlayHost 放在 `src/app/core/`，插件 DLL 需要它的定义与符号，但主程序（.exe）的符号对 DLL 不可见；插件各编一份又变成"每个插件一份实现"；② `OverlayWindow` 头文件里给的典型用法是"继承并重写 `paintOverlay()`"，但宿主没有接收外部实例的接口 → 插件自建的子类永远进不了宿主的登记表，"崩溃后兜底回收"形同虚设 | ① OverlayHost 移入 `src/sdk/`（实现只一份，随 WinEaseSdk 链接进主程序与所有插件；它自身无全局状态）；② 新增 `adoptOverlay(ownerId, overlay)` 作为"插件自建 + 宿主托管"的通道（重复接管只改归属）；③ 插件侧统一"自己 new 子类 → adoptOverlay → show"，停用时 `closeOverlaysOfOwner(id())` |
| 39 | P1-14 壁纸插件**一调用就崩**：`CoTaskMemFree` 相关访问违例 / 进程退出时 `RtlReportCriticalFailure` 堆损坏 | **COM 接口被释放了两次**。取壁纸路径的返回值同时走了两条"释放"路径：`ComPtr`/`QComPtr` 已接管引用计数，代码又手工 `->Release()` 了一次（另外 `SysAllocString` 出来的串被 `CoTaskMemFree`）→ 引用计数被打穿，对象提前析构，随后的调用踩到已释放内存 | 铁律：**`ComPtr` 接管后不再手工 `Release()`**；`CoTaskMemFree` 只用于"文档说明由 CoTaskMem 分配"的返回值，`SysAllocString` 出来的必须 `SysFreeString`。**排查线索**：崩溃栈里出现 `_malloc_base` / `RtlReportCriticalFailure` 且发生在某次 COM 调用之后 → 先查有没有多余的 Release（本次症状是"第一条壁纸断言就崩"，与壁纸逻辑本身无关） |
| 40 | 主题/壁纸插件的"还原"到底该在**退出时**还是**停用时**做？一开始我按"退出即还原"实现，结果和 P1-14 的验收原文（"重启后保持"）直接冲突 | "可完整还原"说的是**别把系统改坏、别留残渣**，而不是"退出程序就要抹掉用户的选择"。主题/壁纸是**用户看得见的外观**，退出时跳变反而是缺陷；真正需要物理清理的是**指向已不存在程序的引用**（右键菜单里写着删除后程序的路径、开机自启项等） | 定契约并写进插件界面与文档：**停用功能 → 恢复成启用前的样子**（这是每条验收的硬指标）；**退出 WinEase → 保持现状**。据此把主题插件里"退出还原"的代码删掉，P1-13/14 统一成同一套语义；P1-05 的右键菜单则**两处都清**（停用清 + 退出清，因为菜单项只在 WinEase 运行时才有意义）。副作用：`feature_smoke` 的系统组用例必须自己负责还原（用例末尾恢复主题/壁纸/注册表），不能指望"退出还原" |
| 41 | P1-05 的「复制路径」在**没有空格**的路径上正常，遇到 `C:\Program Files\...` 就复制出**带引号的字符串**（用户粘出去得手动删引号） | explorer 传 `%1` 时**只有路径含空格才加引号**：`C:\a.txt` / `"C:\Program Files\x.txt"` 两种形态都会出现。命令里自己再套一层引号，反而把**不带引号**的那种变成带引号 | 命令行写成 `cmd.exe /c for %I in (%1) do @echo %~I|clip`（`%~I` 去掉可能存在的引号，两种形态都得到干净路径）。**先做最小实验再写代码**：当时并排试了 3 种写法（自加引号 / `%~` / `%~` + `for`），把含空格与不含空格两种路径各跑一遍才定的方案；自检里同时断言**两种路径**的命令行文本 |
| 42 | P2-12 的覆写回读校验**第一次就失败**（"回读校验不一致（偏移 0）"），而且只报了 65536 = 一块 | `QFile::write()` 之后文件位置停在 `offset + length` 上，紧接着 `file.read(length)` 读的是**下一块**（还是原始内容），拿刚写进去的块跟它比必然不等 | 回读前显式 `file.seek(offset)`。**教训**：同一个句柄上"写→读"必须各自定位，别指望文件位置"回到原处"。这个 bug 恰好被"自检直接断言回读到的字节"抓住——没有那条断言，它会一路装成"粉碎成功"（用户看不到任何异常） |
| 43 | 400×300 的图在 980×700 的预览窗里被**放大**成 1274×956（图糊了、内存白烧） | `QSize::scaled(maxSize, KeepAspectRatio)` 是"**缩放去适配**"：目标框比原图大时它照样**放大**。代码注释写着"只缩小不放大"，判定条件却只用 `target != original`——放大之后当然也不相等，于是判定通过 | 先自己判一次"是不是真的超出目标框"（`original.width() > maxSize.width() \|\| ...`）再决定要不要 `setScaledSize()`。**教训**：Qt 里名字像"缩放适配"的 API 大多会**双向**缩放，"只缩不放"必须自己写条件 |
| 44 | 窗口第一次打开时预览图片，图被缩成**指甲盖**（60×45）贴在大窗口中间 | 解码上限取自 `m_stack->size()`，而 `showFile()` 发生在窗口**首次 show 之前**：布局还没跑过，量到的是"未布局的默认尺寸"（40×30 × DPR 1.5 = 60×45） | 解码前先 `layout()->activate()` 强制跑一遍布局，并对"明显偏小"的结果兜底（退回按窗口尺寸估内容区）。**教训**：**任何"按当前控件尺寸"做的决策都不能在 show 之前做**——不止图片，字号/换行/分页同理 |
| 45 | 批量重命名点完「应用」，状态栏显示的却是"将重命名 0 项，3 项有问题"（刚干完的活看不见了）；面板一打开「撤销上一次」就是可点的 | ① `applyRename()` 先 `setStatus(结果)` 再 `refreshPreview()`，后者会把状态栏改写成"新的计划概要"（它算的是"再点一次会怎样"）；② `refreshPreview()` 在"目录为空/没有匹配文件"时会**早退**，撤销按钮的启用状态（`updateUndoButton()`）没被设置，停在构造时的默认"可用" | ① 结果消息留到最后再写（`refreshPreview()` 之后再 `setStatus()`），`undoLast()` 同理；② 构造时先 `setEnabled(false)`，并在 ctor 补一次 `updateUndoButton()`。**教训**：**"刷新视图"与"报告动作结果"共用一个状态栏时，顺序就是语义**——先刷新、再报结果 |
| 46 | 查重用例断言"保留字典序最小的一份"，素材却把该保留的文件叫 `keep.bin`，于是断言全崩（引擎没错，是素材名字错了） | 名字的**语义**与实现的**排序规则**不是一回事：`dup1.bin < dup2.bin < keep.bin`，字典序最小的其实是 `dup1.bin` | 素材改名 `aaa_keep.bin`，让"该留谁"在断言里一眼可见。**教训**：自检素材**不要用"看起来像语义"的名字**——要么名字直接体现规则，要么断言里把规则写清楚 |
| 47 | 文件组第一次跑，14 条失败里有一批是"读了上一轮的结论"：刚改完控件就断言 | 面板改规则走 **200ms 防抖**（有意为之），而 `waitFor(谓词)` 里的谓词（"「应用」按钮被禁用"）**在上一轮就已经成立**，于是它立刻返回 | 谓词锚在**这一轮独有的特征输出**上（先等状态栏出现"冲突"这类新文本，再断言按钮状态）。**教训**：`waitFor(谓词)` 的谓词必须**只在正确的那一轮成立**，否则它就是个昂贵的 `sleep(0)`，会让整组断言看起来随机失败 |
| 48 | 端到端用例要"点插件面板上的按钮"，可测试连插件的头文件都用不了 | 插件是独立 DLL，**不导出 C++ 符号**（没有 `dllexport`），所以 `plugin->panelWindow()` 这类具体类上的访问器在测试里**链接不过**；拿到的只有 `IFeaturePlugin*` | 统一按 **`objectName` + `QApplication::topLevelWidgets()`** 找顶层窗口，再 `findChild<T*>(name)` 取控件（`QWidget::findChild` 走 Qt 运行期元对象，跨 DLL 天然可用）。**约定**：面板里凡是要被自检驱动的控件都要 `setObjectName("<功能前缀><控件>")`（如 `dupFinderStart` / `shredderAcknowledge`）；面板类**不为了测试加 `Q_OBJECT`**（.cpp 内的类要单独 moc，不值当），要向上报状态就直接调插件的公开方法（`reportStatus()`） |
| 49 | P2-04 的"删掉/清空后磁盘上还剩一个正文文件"：历史文件数与 `payload/` 里的文件数**始终差 1**，而索引本身完全正确（插件逻辑看起来没问题） | **先写文件、后进索引**的顺序在"不进索引"的那条路上留了尾巴：`addText()` 先按新 id 把正文写进 `payload/`，再交给 `insert()` 去重；而 `insert()` 一旦判定"同样内容已存在"（`Replaced`）就用**旧条目**（旧 id、旧正文文件）提到最前 —— 刚写下去的那份**没有任何人再引用它**，却已经躺在磁盘上了。这个 bug 只有在"复制了同样的内容两次"时才会显形，而它的后果是**磁盘上永久残留用户复制过的内容**（对剪贴板历史这种功能来说，这既是垃圾也是隐私问题） | 把纪律写死在 `Store::insert()` 头部：**调用方已经把正文写好了，所以本函数只要不走"用这份 entry"，就必须删掉那份正文**（`removePayloadFile(entry)`，且**先删再写索引**——索引从不引用它，删早了不会丢数据，删晚了才留垃圾）；`save()` 失败那条路同样要把新写的正文删掉并回滚内存里的 prepend。**教训**：凡是"先落副作用、后做决策"的写法，都要逐个走一遍**所有放弃决策的分支**，问一句"我刚做的那件事谁来撤销"；自检里"索引条数 == payload 文件数"这一条等式就是专门抓这类尾巴的 |
| 50 | P2-04 面板页脚上"隐私已跳过 N 条 / 因超上限跳过 N 条"在自检里**不跟着动**（值是对的，只是不刷新） | 面板的刷新只挂在 `historyChanged` 上，而"跳过"这件事**恰恰意味着历史没变** → 信号不发、页脚不动。用户视角的后果：面板开着的时候复制了一条密码，界面上看不到任何反馈（"它到底拦住了没有？"）—— 而这正是本功能最需要说清的一件事 | 拆出语义更准的信号 **`statisticsChanged()`**（"统计变了但内容没变"），隐私跳过、超上限跳过、写失败三处都发，面板照旧连到 `refresh()`。**教训**：把"内容变化"与"统计信息变化"当同一个信号用，就会在"发生了事情但内容没变"时静默丢失刷新；**面板上写出来的每个数字，都要有人负责在它变化时通知面板** |
| 51 | P2-09 放大镜"启用"成功、面板也开了，可面板上那个「开始放大」按钮**按不动**（点下去没反应，功能看起来是坏的） | 插件在 `onEnable()` 里调 `refreshPanel()`，而 `refreshPanel()` 按 `isEnabled()` 决定按钮可用状态；宿主 `setPluginEnabled()` 的顺序是**先 `onEnable()`、后落"已启用"状态**，于是插件此刻读到的 `isEnabled()` 还是 `false` → 按钮被刷成禁用，而且再没有任何时机去纠正它（面板不会自己再刷） | `onEnable()`/`onDisable()` 末尾补一发 `QMetaObject::invokeMethod(this, [this]{ refreshPanel(); }, Qt::QueuedConnection)`（**排到状态落定之后再刷**），同步调用保留（面板刚建出来时的即时状态仍要正确）。**教训**：**"启用/停用回调里读到的自身状态是旧值"** —— 凡是在回调里根据 `isEnabled()` 决定控件状态的，都要问一句"这时候它已经改过了吗"；自检也要把"点下去没反应"当成缺陷报出来（本案就是自检的"按钮点击后窗口没出现"抓到的） |
| 52 | P2-09 的"CPU 占用明显低于自抓屏方案"一度**量不出来**：40 帧跟随测出 `0 ms`，参考实现 `15 ms` —— 数字看着漂亮，其实两边都在噪声里 | ① `GetProcessTimes` 的毫秒由**时钟中断**驱动（本机约 15.6ms 一跳），"40 帧跟随"这种量级的真实成本会被四舍五入成 0 —— 用 0 去比大小是**假通过**；② 同样的时长里，**事件循环本身**（Qt 定时器、宿主刷新、自检进程）也在烧 CPU，不扣掉它就会把宿主开销算到被测功能头上 | 比大小改用 **`QueryProcessCycleTime`（CPU 周期，精确）**：先测"关掉放大镜、同样时长"的空转基线，再测"跟随同样时长"，**差值**与参考实现比；毫秒只保留一条宽松的"占比上限"断言，并在断言文案里注明分辨率（`0 ms = 低于分辨率`）。**教训**：性能对照的**测量单位必须比被测目标细一个量级**，否则"测不出来"会被当成"很省"。<br>（续）单位选对了还有第二层坑：**对照必须"同工同酬"** —— ① 参考实现只把缩放结果画进内存 DC、**从不呈现到屏幕上**，等于凭空少干一半活（补上"呈现到真实窗口"这一步后成本 +37%）；② 我们的放大镜在光标不动时**故意跳过**重复设源区，那是省下来的活，拿"静止档"去比"每帧都必须重抓"的自抓屏也不公平。可比的是**双方都干满活的那一档**。最后一条：**断言只能写真实成立的事** —— 量出来是"同量级（略高）"就断言"同量级"，把"原验收标准不成立"写进 INFO 与验收说明，不要为了好看留一条会变红的断言 |
| 53 | P2-08 调色温时 `SetDeviceGammaRamp` 返回 FALSE，报出的原因却是 **"设置 gamma ramp 失败：成功（错误码 0）"**；同一台机器上 4000K 能写、3200K 写不进去 | Windows 对 gamma ramp 的幅度有**硬校验**：幅度超出范围时 `GdiSetDeviceGammaRamp` **静默拒绝**（返回 FALSE 但**不设置 last error**）。原代码直接 `describeFailure(..., lastError())`，于是"错误码 0"被翻译成了"成功"。实测阈值与"每通道 ramp 最大值不得低于约 **32768（0x8000）**"吻合：3250K 的蓝通道末级 32459 < 32768 被拒、3500K 的 36208 通过 | ① 失败且 `lastError() == ERROR_SUCCESS` 时改报**明确文案**（"设置 gamma ramp 被系统拒绝（Windows 限制了应用程序可调节的幅度）"），别再把"成功"当原因；② **护眼模式的目标色温不能写死理想值** —— 自检先**探测本机真实可写范围**（逐档试写，第一个失败即下限），再拿实测档位做断言，并额外断言"被系统拒绝的档位必须如实报错、且不留下半套滤镜"。**教训**：`GetLastError()` 为 0 的失败是真实存在的（尤其 win32k 的静默拒绝），凡"失败 + 错误码 0"都要单独给一个人话解释 |
| 54 | P2-08 写内置屏亮度时卡在 `WmiMonitorBrightnessMethods 没有 InParameters 模板`（读回亮度是好的，只有写不通） | `IWbemClassObject::Get(L"InParameters", ...)` 只在**直接对方法对象**操作时才拿得到签名；对**类定义**调用它取不到任何东西（返回 `WBEM_E_NOT_FOUND`）。正确入口是 `IWbemClassObject::GetMethod(L"WmiSetBrightness", 0, &inDef, &outDef)` | 用 `GetMethod` 取参数模板 → `SpawnInstance` 派生可写实例 → `Put("Timeout"/"Brightness")` → `ExecMethod(path, "WmiSetBrightness", ...)`。另外 `Brightness` 的类型是 `CIM_UINT8`（`VT_UI1`）、`Timeout` 是 `CIM_UINT32`。**教训**：WMI 的"写方法"三步（GetMethod → SpawnInstance → ExecMethod）与"读属性"完全不同，别拿读的那套往里套 |
| 55 | P2-07 自检里"第二条电源任务到点后请求没发出去"，`waitFor` 12 秒超时；同时倒计时秒数出现"跳回去"的现象 | 两条电源任务在同一拍里都到点时（自检预置的两条 `nextFireAt` 都在过去），第二条的 `startPowerCountdown()` 直接 `begin()` 覆盖了第一条正在走的倒计时：① 用户看到的剩余秒数莫名重置；② 覆盖之后**没有任何记录说"上一条被吃掉了"**，用户设的关机可能永远不发生 | ① `startPowerCountdown()` 改为返回"本任务是否被处理"；已有倒计时在走时**不覆盖、不静默丢弃**：如实说一句"已有电源倒计时在进行中，本任务稍后自动重试"，并**不推进**这条任务的触发时刻（它保持"待触发"，当前倒计时结束后下一拍会被 `dueTasks()` 重新捞出来）；② 调用方据返回值决定 `advanceTask()` 要不要执行。**教训**：凡"同一时刻可能来两个请求、而资源只能有一个"的地方，都要明确回答"第二个去哪了"——覆盖与静默丢弃是最容易被写出来的两个错误答案 |
| 56 | P2-07 自检断言"收尾：麦克风静音状态已还原为用户原值"**失败**（原值未静音 / 现在静音），可 `MicMuteGuard` 析构里明明写着还原 | 守卫只实现了"析构时还原"（RAII），而断言写在**守卫析构之前** —— 验到的是"还没还原"的状态。RAII 保证的是"函数退出时一定还原"，它**不保证"在函数体内的任意时刻都已经还原"** | 守卫补一个显式的 `restore()`（幂等）并让析构复用；用例改成**先 `restore()` 再断言**。**教训**：`MicMuteGuard` / `ClipboardGuard` 这类 RAII 守卫，"兜底"与"断言依据"是两件事 —— 凡是要在用例里**断言**副作用已还原，就必须先显式触发还原动作 |
| 57 | P2-11 自检报"没弹气泡提示"，可面板徽标亮着、状态文本也对；同一份代码在下一轮轮询（1 秒后）又把通知补上了 | **又是坑 #51 的变体**：`onEnable()` 里**同步**调 `poll()`，而 `poll()` 用 `isEnabled()` 决定"能不能弹通知" —— 那一刻宿主的"已启用"状态还没落定，`isEnabled()` 仍是 `false`，于是徽标亮了、气泡没弹。后果不只是自检失败：**用户启用功能时正好有人在用摄像头**（恰恰是最需要提醒的场景），那次提醒会被吞掉，只能等下一轮（默认 3 秒） | 把 `onEnable()` 里的首次检查排到事件循环下一轮（`QMetaObject::invokeMethod(..., Qt::QueuedConnection)`），与 `refreshPanel()` 同一手法。**教训**：**"回调里读到的自身状态是旧值"这条坑已经出现过三次（#51 / #28 / #57）** —— 凡是 `onEnable`/`onDisable` 里要读 `isEnabled()`（或任何由宿主在回调**之后**才落定的状态）的代码，一律先问"这时候它改过来了吗"，拿不准就排到下一轮 |
| 58 | P2-13 自检报"中文主机名应当被判错"失败：`10.0.0.1 站点.com` 被当成了**合法记录** | 校验主机名时图省事写成了 `QChar::isLetterOrNumber()` —— 它对**中日韩汉字、全角字符一律返回 true**（它问的是"这是不是字母"，不是"这是不是 ASCII 字母"）。后果正是这个功能最想拦住的那种错误：用中文输入法打出来的域名被放行、写进 hosts，然后**永远解析不了**，用户只会觉得"我明明写了怎么不生效" | 主机名只接受 **ASCII**：`0-9 a-z A-Z` 加 `-` `_`，逐字符比较 `QLatin1Char` 范围。**教训**：凡是"必须有 ASCII 语义"的校验（主机名 / 变量名 / 注册表值名 / 协议字段），都不要用 `isLetter()` / `isLetterOrNumber()` / `isPrint()` 这类**语言相关**的判断 —— 它们的"字母"包含全世界 |
| 59 | P2-13 自检断言"保存用的是 replace 模式"与"送出的字节逐字节一致"同时失败，`contentBase64` 是空的；可上一条"保存请求真的发出去了"却是通过的 | 自检读的是 `lastArguments()`（**最后一次调用**的参数），而"保存成功"这个业务动作会**带出一串调用**：`writeHosts` → 成功后插件立刻 `hostsBackups list` 去刷新备份列表 → "最后一次调用"变成了那条列表请求，参数当然对不上。同类的连锁调用还有"改动后自动重新探测""写完后立刻回读"等 | 记录型桩新增 **`lastArgumentsOf(operation)`**（按操作找最近一次），断言一律用它。**教训**：桩的断言入口要按**语义**取（哪一类操作），不要按"时间上的最后一个"取 —— 一个用户动作往往对应多个系统调用，这个事实在写桩的时候很容易被忽略 |
| 60 | P2-14 自检报"保存后变量表自动刷新到最新状态"失败（表 4 行 / 注册表 5 项）：变量确实写进去了、回读也对，就是**列表没变** | `saveVariable()` 成功后调 `reloadFromRegistry()` 重新读了数据，但**重绘变量表的那段代码在 `refreshPanel()` 里**，而这条路径没有调用它 —— 数据是新的、界面是旧的。用户视角就是"点了保存，什么都没发生"（然后他会再点一次、再点一次……） | `reloadFromRegistry()` 末尾补 `refreshPanel()`。**教训**：凡是"改完数据 → 重新读 → 展示给用户"的路径，都要问一句**"重绘在哪一步"** —— 数据加载与界面刷新被拆进两个函数时，漏掉后者的错误不会报错、不会有异常，只会静默地让用户以为操作失败（与踩坑 #21「刷新视图与报告结果的顺序」同源） |
| 61 | P3-14 自检跑到新建的 P3 组时**整个进程崩掉**，退出码 `-1073741571`（= `0xC00000FD`，**栈溢出**），一行断言都没输出 | **经典信号递归**：`refreshPanel()` 会重填设备列表（`clear()` + `setCurrentRow()`），而这两件事都会发出 `currentRowChanged`；该信号连的槽里又调了 `refreshPanel()` → 递归到栈耗尽。**用户打开面板同样会崩**（不是自检特有的问题） | ① 重填列表期间用 `QSignalBlocker` 屏蔽信号；② 槽里**只做该做的事**（刷新详情），不要顺手"刷新整个面板"—— 选中项变化根本不影响任何按钮的启用状态。**教训**：凡是"程序化修改控件 → 该控件的信号 → 回调又修改该控件"的链条，都要问一句"这个信号会不会把我自己叫回来"；**用 `QSignalBlocker` 是标准解**，把回调缩到最小是更彻底的解（P3-02 直接把"重填列表"与"改按钮状态"拆成两个函数） |
| 62 | ★★ P3-02 自检报"自检既不是关键进程、也在当前会话"失败：`isCritical=1` —— **自检自己**（一个普通控制台程序）被 Restart Manager 标成了关键进程。而如果照这个判据实现，"不允许结束"的保护规则就会把普通程序也罩进去 | **`RmCritical`(=1000) 不是"系统关键进程"的判据**。官方定义是"**这单装不下去了，得重启系统才能释放**"，成因有三种：① 它确实是关键进程；② **当前进程没权限关它**；③ **它就是发起查询的这个程序自己**（Restart Manager 不可能建议"把你自己关掉"）。**本机实测**：同一份 `feature_smoke.exe`，作为**查询方**被标 `type=1000`，作为**普通子进程**被标 `type=5`（控制台程序）。数值 `1000` 本身就在 0–5 的连续区间之外 —— 它是个"特殊状态标记"，不是分类 | 判据改成问「**它是不是操作系统的组成部分**」：WinEase 自己 / 提权助手 / 一小份 system-owned 名单（`System`/`smss.exe`/`csrss.exe`/`lsass.exe`…）。这些进程同时也是**受保护进程**，放行也只会拿到"拒绝访问"，名单的价值是把那句话翻译成人话。`RmCritical` 只保留一句**如实提示**（"系统认为它关不掉、要重启才能释放 —— 可能只是权限不足，**不等于**它是系统关键进程"）。**教训**：把某个 API 的返回值直接当成"业务结论"之前，先读清它的**定义与成因**（尤其是"成因里有好几条"的那种）；这类错误的方向很危险 —— **它会把能做的功能变成做不了**，而且表现成"功能看着正常、只是怎么点都没反应" |
| 63 | 编译出现 `warning C4305 / C4309（截断常量值）`，指向 `QLatin1Char('·')` | `'·'`（U+00B7）在源码里是多字节字符常量，类型是 `int`（值 `0xC2B7`）；传给 `QLatin1Char(char)` 会被**静默截断**成 `0xB7`。截断后的值这次**恰好是对的**（Latin-1 的 `·` 就是 0xB7），所以功能没问题、只有警告 | 写 `QChar(0x00B7)`。**教训**：非 ASCII 的字符常量一律写 `QChar(0xXXXX)` / `QStringLiteral`，别用 `QLatin1Char('…')` 去装多字节字符 —— "这次恰好对"不是理由，它同时还会污染"0 warning"这条基线 |
| 64 | ★★ P3-01 的桌面列表里出现**两个"当前桌面"**（其中一个只有 UWP 窗口），以及一条写着 `其它桌面（4 个窗口）{00000000}` 的**幽灵桌面** | `IsWindowOnCurrentVirtualDesktop` **单独用不可靠**：**被 DWM 隐去（cloaked）的窗口会回答"是"**；而 `GetWindowDesktopId` 对某些窗口（UWP 宿主、工具窗口）会"成功"返回**全零 GUID**。初版把"第一个回答为真的窗口"当作该桌面的当前状态、又把全零 GUID 当成一个桌面 → 列表里两条"当前桌面"、外加一个点了只会报"目标桌面标识非法"的幽灵项 | ① "当前桌面"**以"前台窗口所在桌面"为准**（它按定义就在用户正看着的那个桌面上），拿不到时才退化成"**未被隐去** + 回答为真"的投票，并且**只认一个**；② 查询时**直接跳过 `GUID_NULL`**（它不是桌面）。自检新增一条硬断言"**恰好有一个**桌面被标为当前"，以及一条 `isValidDesktopId` 的对照。**教训**：`IsWindowOnCurrentVirtualDesktop` 这种"会回答你但可能答错"的接口，必须再找一个**语义上不可能错**的信号（这里是前台窗口）来定案 |
| 65 | ★★ P3-01 想测"自己的窗口能不能搬"，却先后拿到两个**假结论**：① `settings->winId()`（创建了但**从没显示**的窗口）→ "不属于任何虚拟桌面"；② 自检常用的 `SolidColorWindow`（`Qt::Tool` + 无边框）→ 同样"不属于任何虚拟桌面"。差点写成"连 WinEase 自己的窗口都搬不动，功能不可用" | **外壳只跟踪"应用窗口"**：从没显示过的窗口、`WS_EX_TOOLWINDOW`（工具窗口）、无边框浮窗、桌面/任务栏这类外壳窗口，都不属于任何虚拟桌面 —— `GetWindowDesktopId` 对它们返回 `TYPE_E_ELEMENTNOTFOUND`。**这不是 API 失效，是"样本选错了"**（与预研时拿 shell 窗口当目标踩的是同一个坑，#46） | 基准样本改用**普通应用窗口**（有标题栏、进 Alt+Tab 的那种：自检里现场 show 一个 `QWidget`）。把这条观测写进自检 INFO。**教训**：验"能不能做某事"之前，先确认**手里的样本是这件事的合法对象**；"用错样本"和"能力不存在"会给出一模一样的失败信息，而前者会把好消息误判成坏消息（#46 的同族坑）|
| 66 | ★★ P3-01 的核心功能靠**实测**才定了性：`MoveWindowToDesktop` 能不能搬**别的进程**的窗口，官方文档**只写"成功返回 S_OK"**，一个字没提权限 | 用"把窗口搬到**它自己所在的桌面**"当探针 —— **成功与失败都不改变任何东西，却正好回答"系统让不让我碰这个窗口"**。三档结果：自己的窗口 `S_OK`、探针子进程的窗口 `E_ACCESSDENIED`、第三方程序的窗口 `E_ACCESSDENIED` | 于是"跨桌面搬迁"这条验收**降级为"只搬本进程窗口 + 如实报错"**（裁剪记录 C10）。**教训**：**要测"有没有权限"，就用一次"不改变任何东西的调用"去问** —— 这比"先真做一次、失败再回滚"安全得多，也让"只读探针"这个纪律能覆盖到"权限类事实"；另外，**文档缺失的边界必须自己测出来再写进产品**，否则用户看到的就是"点了没反应" |
| 67 | ★★ P1-04 的"前置：子进程成功把辅助窗口切到前台"**偶发失败**（同一份二进制，有时过有时不过），跟着的"z 序维持生效"就变成**假通过** | 判定写成"**某一次**采样等于目标窗口"：前台切换是**别的进程**做的事，中间必然经过瞬态（"目标窗口 → 辅助窗口"），一次命中就往下走，后面的断言就做在一个**还没稳下来**的状态上；而 `SetForegroundWindow` 的返回值也不等于"前台已经是它了" | 统一改成**连续稳定**判定（`waitForStable`：超时内**连续 3 次**采样都保持目标、采样间隔 20~40ms、每次采样之间处理 Qt 事件），并把前台切换做成**分阶段**（直接试 → 立即检查 → 等事件循环 → `AttachThreadInput` 再试 → 交给连续稳定确认）；`ProbeWindow::requestRaise()` 两次 ×1500ms 后仍失败就**如实报前置不成立**（不无限重试、不静默跳过）。**教训**：凡是"另一个进程在改、且会经过瞬态"的状态，判据都必须是"**连续稳定**"而不是"某一次命中"；另外**别把不属于本功能的信号当证据**（这条用例原先隐含依赖"前台没变"） |
| 68 | ★★ P3-01 自检报"焦点保护：目标不是活动窗口时按钮应保持禁用"**失败**（按钮明明是启用的），而插件的逻辑看代码是对的 | 自检里那句解锁写成了 `dispatchAction(..., "unlock")`，但 `WindowFeatureState::handleLockAction` **只认 `"lock"` 这一个动作名**（它本身就是**开关**：锁/解锁都用它）→ `"unlock"` 没被消费，**掉进了插件的默认动作**（真的去搬窗口），而且目标一直停在"锁定"状态 → 插件眼里的"目标窗口"根本不是鼠标下的那个 | 用例里**锁/解锁都用 `"lock"`**，并断言状态文本（"已锁定"/"已解除锁定"）证明动作**真的被消费**。**教训**：`dispatchAction` 返回 true 只说明"快捷键被分发出去了"，**不代表插件处理了它** —— 动作名写错时它会静默落到默认分支（这类"看起来成功、其实走了另一条路"的错误最难查）；凡"动作名只有一个"的约定，就照它写，别自己发明第二个 |
| 69 | P1-08 的"停用后屏幕像素**逐像素恢复**"偶发变红：`#3366CC → #3264C7`（每通道差 1~5），两次复现数值完全一样，隔一轮又完全正常 | 判据用了**严格相等**，而分层窗口被回收后的最初几帧由 DWM 合成，存在**时序噪声**（实测偏差 1~5/255，重跑即消失）。真残影（整层压暗 / 圆环描边）的量级是 **≥30~40/255**，两者差一个量级 | 判据改成"**每通道 ≤8/255**"（对真残影仍有 4 倍以上余量），并把诊断一次打全：邻域像素、该点顶层窗口、背景板句柄、光标位置、**把光标移开后再读一次**、探针窗口矩形。**教训**：像素类断言要么给"有据可依的小容差"，要么先把诊断打全再判 —— 一个 2/255 的噪声就把整条用例变红，会让人开始怀疑被测代码；但**容差必须能证明它对真问题是灵敏的**（这里是 4 倍余量） |
| 70 | ★★ P3-03 批量格式转换**每一张图**都以"改名失败"收场：临时文件明明写好了（`QTemporaryFile` → `rename` 到正式名），而引擎里那段"写完再改名"的逻辑看上去毫无问题 | `QImageWriter` 写完之后**没有及时析构** —— 它一直活到 `QFile::rename` 那一刻。Windows 上"**自己进程还开着的文件**"根本**改不了名**（POSIX 可以，所以这类代码从 Linux 上游抄过来必然踩），`rename` 一律失败 → 每张图都"写成功了、却装不上正式名字" | 写完立刻结束 writer 的作用域（`{ QImageWriter writer(...); writer.write(img); }`，必要时再 `writer.setDevice(nullptr)`），**再**改名。自检断言"**输出文件存在 + 像素/字节与预期一致**"。**教训**：平台差异不只在 API 存在与否，还在**句柄生命周期**上 —— "能读到"≠"能改名"，凡"写临时文件再改名"的落盘方式，都要先问"此刻还有谁开着它"；另外**只断言"函数返回 true"会漏掉这类"报了错但没崩"的失败**，落盘类功能的断言必须落到**产物**上 |
| 71 | ★★ P3-03 的"跑完一批、再跑第二批"让**整个进程当场消失**（没有失败框、没有 `[FAIL]`，退出码异常），而图片侧那条路径从没复现过 | `m_worker = std::thread(...)`：`std::thread` 的**赋值运算符**碰上"自己还 joinable"会**直接 `std::terminate`** —— 不抛异常、不覆盖，进程立即消失。第一批跑完后线程对象一直没人 join（设计上"开工期间「开始」是灰的"，所以只有**开新批次**时才需要收尸）。图片用例没踩到是因为它的第二批**全被"跳过"策略排空了、压根没开线程** —— 崩溃正好躲在那条"平时走不到"的支路上 | 开新线程前统一走 `reapFinishedWorker()`（`if (m_worker.joinable()) m_worker.join();`，两处开线程的地方都调），并在自检里加一条**真跑第二批**的回归（第二批的输出**逐字节**与第一批一致）。**教训**：长生命周期对象里的 `std::thread`/`QThread` 成员，**"赋值前先收尸"是硬纪律**（同族：#28/#51/#57 的"回调里读到旧值"也是"对象生命周期没想清"）；而"被策略/前置条件排空的支路"正是崩溃最爱的藏身处 —— 自检要**主动把第二批跑起来**，而不是让它被静默跳过 |
| 72 | ★★ P3-07 的磁盘温度**读不到**：`IOCTL_STORAGE_QUERY_PROPERTY` 返回 `ERROR_INVALID_FUNCTION`（面板于是显示"该磁盘或驱动不提供温度属性"），而 `p3_spike` 用同一条路径（`\\.\PhysicalDrive0`）**明明读到 40°C** | 属性 ID 是**手抄的数字**：代码里写 `static_cast<STORAGE_PROPERTY_ID>(22)`（注释还写着"兼容较旧 SDK"），而**本机 SDK 的 `StorageDeviceTemperatureProperty` 是 `52`** —— 文档/记忆里的枚举顺序早已过期，22 指向的是别的属性，驱动自然"不认识这个查询"。排查时先怀疑了路径（SetupDi 接口路径 vs `\\.\PhysicalDriveN`）、缓冲区大小（1024 vs 512）、调用顺序（先查型号会不会影响温度）三条**全都错**；真正的定位手段是**在同一段代码里并行跑两个版本**（SDK 枚举 / 手抄常量）并把两者的 `ok/err/温度` 一起打出来 —— 一条日志就把范围锁死在"常量值"上 | **Windows 的枚举值一律用 SDK 枚举名，绝不手抄数字**（枚举值随 SDK 版本会变，只有名字是契约；真要兼容旧 SDK 就 `#ifdef` + `#error` 逼人去查，而不是填一个"记错了也没人知道"的数）。另一条：**`ERROR_INVALID_FUNCTION` 不等于"我们参数错了"**，它的常见含义是"驱动不处理这个查询"，直译成"函数不正确（参数或调用方式有误）"会把用户往沟里带 → 错误文案按**语义**翻译（"该磁盘或驱动不提供温度属性"）。**教训**：对照实验要**把怀疑对象做成变量**（同一进程、同一时刻、只差一个参数），比"改一处置信、重编译、再猜下一个"快得多；排查过程里写下的**未经验证的机理**（"接口路径开的是 PDO 所以不行"）**不许留在注释和文档里** —— 那会变成下一个人的错误前提 |
| 73 | ★★ P3-07 的两个全局快捷键（`Ctrl+Shift+Alt+I` / `Ctrl+Shift+Alt+R`）**按下去毫无反应**，而自检里 `dispatchAction()` 返回 true、插件代码看不出任何问题 —— 只有"复位后面板位置真的变了"这条断言报了红 | 宿主分发给插件的 `onHotkey(hotkeyId)` 传的是**完整快捷键 id**（`monitor.hardware_hud::interactive`），而插件把它直接和动作名 `"interactive"` 比 —— 永远比不中，两个分支**一个都不进**（与 #68 同族："分发成功 ≠ 插件处理了它"，这次是"连分发的形状都没对上"） | 统一取动作名：`hotkeyId.section("::", 1, 1)`（与 `AlwaysOnTopPlugin` 等既有插件同一写法）。**教训**：① **拿到回调参数先确认它的形状**（是动作名还是完整 id），别照着"我以为"写；② 快捷键类功能**必须靠"外在状态真的变了"来断言**（位置真的落在右上角 / 扩展样式真的少了 `WS_EX_TRANSPARENT`），只断言"函数返回 true"或"状态栏有提示"会放过整类"按了没反应"的 bug；③ 自查同类插件的动作名取法，避免"只有这一个插件是错的" |
| 75 | ★★ P3-11 媒体控制面板：读回来的**时长/位置差 10000 倍**（3 分钟的曲目读成 1800 万毫秒），而代码里看着一切正常 | WinRT 的 `TimeSpan` / `DateTime` 单位是 **100ns tick**，不是毫秒（`Position()`、`EndTime()`、`LastUpdatedTime()` 全是）。同族的还有枚举：播放状态要用 `GlobalSystemMediaTransportControlsSessionPlaybackStatus`（`Closed/Opened/Changing/Stopped/Playing/Paused`，`Playing` 排第 **4**），而名字更像"正牌"的 `Windows::Media::MediaPlaybackStatus` 是 SMTC **发布方**的枚举（`Playing` 排第 **3**）—— 一旦用 `static_cast` 把编译错误糊过去，状态就会**静默错位**（把"已暂停"显示成"正在播放"） | 单位换算集中在平台层一处常量（`kTicksPerMs = 10000`），层内不出现第二个换算点；枚举类型就用 SDK 里被调用者真正返回的那个（见 `MediaSessions.h` 的注释），**不许 `static_cast` 绕编译错**。自检用"发布方自己声明的 3:00"反查回读值（差 10000 倍当场红）——"单位对不对"这种事只有**拿对方的尺子量回来的数**才验得出 |
| 76 | 编译报错出现在**Qt 头文件里**：`qarraydataops.h` 报 `winrt::Windows::Foundation::IUnknown::operator new 函数不接受 3 个参数`，指着一段和我无关的容器代码 | C++/WinRT 的**投影 runtime class 自带 `operator new`**，而 Qt 容器在存储里是用 placement new 构造元素的 —— 两者一碰就编不过（报错位置在 Qt 里，根因却在我的容器用法上） | 不把 WinRT 对象放进 Qt 容器（`QList`/`QVector` 都不行）：改成"一次遍历 + 回调"把需要的东西当场取出来（`MediaSessions.cpp` 的 `visitSessions`），需要"找到它之后再操作"就在同一个遍历里完成。**教训**：报错位置在第三方的头文件里时，先看**我自己传进去的类型**有没有特殊构造/析构语义，别去改对方的代码 |
| 77 | ★★ P3-11 的探针子进程**一调用就消失**：没有任何输出、没有 `[失败]`、退出码 `-1073741819`（0xC0000005），插了检查点才把范围收窄到 `interop->GetForWindow(...)` 这一行 | 手写 WinRT 互操作接口声明时把基类写成了 `::IUnknown`，而 WinRT 接口实际派生自 **`IInspectable`**（多 `GetIids/GetRuntimeClassName/GetTrustLevel` 三个槽）→ vtable 整体前移 3 个槽，`GetForWindow` 调到了 `GetIids` 上，参数/返回值全错位。同族第二个坑：投影 runtime class **不能用 `winrt::put_abi(obj)` 当 out 参数** —— 它不是"一个接口指针"，把指针写进对象自己的地址上就是内存踩踏（症状同样是 0xC0000005） | 手写声明一律 `struct __declspec(uuid("...")) IXxx : ::IInspectable`（对照 SDK 头文件的真实基类；本项目 `VirtualDesktop.cpp` 里也有同类手写声明可参照）；取互操作对象改用"先拿裸指针 + `winrt::take_ownership_from_abi` 交给投影对象"。**教训**：C++/WinRT 的崩溃常常**不带任何诊断**，最快的定位手段是**在每一步之间插一行 printf**（起点 → 设 AUMID → 建窗口 → 取工厂 → GetForWindow），一次运行就把范围锁到某一行，比反复猜快一个量级 |
| 78 | ★★ P3-11 读媒体封面**卡死**：`mediaSessionArtwork()` 15 秒不返回，进程 CPU 接近 0（不是忙等，是死等），而系统里那份封面明明存在 | `IRandomAccessStreamReference::OpenReadAsync()` 的**完成回调要回到"调用线程的公寓"**；在 **STA** 线程上 `.get()`，如果那个线程没有消息循环可泵（自检在 `main()` 里顺序跑各用例、主程序这一层也还没进事件循环），完成永远回不来。**同一份代码放到 MTA 工作线程上 200ms 就返回** —— 对照实验（同一进程、同一时刻、只差"在哪个线程调"）一次就把根因钉死 | 封面读取在平台层**内部**换到自建的 MTA 工作线程（`ComApartment::initialize(ApartmentModel::MultiThreaded)`），调用方在界面线程或自检里调都不会死；自检则把"**封面中心像素的颜色**"作为断言（真数据，不是"非空就行"）。**教训**：`.get()` 同步等 WinRT 异步操作，**能不能等取决于"完成回调要回哪个公寓"** —— 系统自带、跨进程敏捷的操作（如 `TryGetMediaPropertiesAsync`）在哪都能等，**跟文件流/自定义流打交道的那些**（`OpenReadAsync`）在没消息循环的 STA 上会死等；凡是"读外部对象"的异步操作，默认放到工作线程 |
| 79 | P3-11 的自检里注册媒体会话失败：`HRESULT 0x800700A1`（`ERROR_BAD_PATHNAME`），而**手工用同一条命令、同一段代码**跑却一切正常 | `StorageFile::GetFileFromPathAsync()` **只认反斜杠路径**，而自检的路径是 Qt 的 `QDir::tempPath()` 拼出来的 —— 那是 `C:/Users/...` **正斜杠**。手工复现时用的是 PowerShell 的 `$env:TEMP\x.txt`（反斜杠），于是"手工能跑、自检必挂" | 交给 WinRT 之前一律 `QDir::toNativeSeparators()`；同时把"封面发布"单独 try/catch 兜住 —— **封面是附加信息，不该把整个会话注册一起拖下水**（真播放器也是"没封面照样能播"）。**教训**：桌面 API 对路径分隔符的容忍度**不一致**（Win32 API 基本都认正斜杠，Shell/WinRT 的 `StorageFile` 不一定），跨层传路径时以"原生分隔符"为准；另外"手工能跑、自检挂"这种差异，第一件事是**把手工命令换成自检里那个参数形状** |
| 80 | ★★ P3-11 自检造的媒体会话："暂停请求被接受"（`TryPauseAsync()` 返回 true），但**回读播放状态仍然是"正在播放"** —— 差一点就把"控制链路通不通"测成"函数返回 true 就算过" | SMTC 的 `TryXxxAsync()` 只是把**请求交给播放器**（转成应用的 `ButtonPressed` / `PlaybackPositionChangeRequested` 事件），**播放状态是播放器自己改的**。自检造的假播放器没接这些事件，自然永远是发布时那句"正在播放"（真实播放器会改） | 探针接管 `ButtonPressed` 与 `PlaybackPositionChangeRequested`：**把收到的事件逐条落盘**（这是"请求确实送到了播放器"唯一的硬证据）并**真的改自己的状态/位置**；父进程再回读状态与位置。于是"命令 → 系统 → 播放器收到 → 播放器改状态 → 我们读到"整条链路都被钉住 —— 只断言"返回 true"等于什么都没验（同族：#73） |
| 74 | ★★ P3-09 音量混合器：**每一个应用都被多报一份会话** —— 只有一个音频会话的应用在面板上显示"2 个会话"、tooltip 里还弹出"该应用在这一台输出设备上开了 2 份会话…"；更隐蔽的是"显示已过期的会话"里过期行**永远算不出 expired**（滑块不禁用、副标题不写"已过期"） | `MixerRow::sessionCount` 的**默认值写成了 1**，而 `buildSnapshot()` 是"每收一份会话就 `++`" —— N 份会话累加出 N+1。同一个错误还把 `row.expired = (expiredCount == sessionCount)` 的等式打破（1 ≠ 2），于是"整行都过期"这个判定永不成立 | 默认值改 **0**（计数就该从 0 起步），并在 `MixerModel.h` 写明"由 buildSnapshot 累加得出"、在 `accumulate()` 旁注明原因。自检侧加三条断言把这件事钉死：**两份会话 → `sessionCount` 恰好是 2**、副标题恰好含"2 个会话"、**含过期会话的行必须 `expired == true` 且不可调**。**教训**：① "字段默认值 + 累加"这种写法里，默认值就是**语义** —— 写 1 等于把"空行"伪装成"有一份会话"，凡是计数字段都要问一句"从几起步、谁负责 +1"；② 这类 off-by-one **不报错也不崩**，只会让界面上的话变成假话（用户看到"2 个会话"却只找到一个播放器），只有"拿 N 份真实会话断言恰好等于 N"才抓得住 —— 而本组的 N 是自检**自己起子进程**造出来的两份真实会话，靠"系统声音"那一份是永远验不到的 |
