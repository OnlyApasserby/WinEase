# ============================================================================
#  P1-03 窗口透明度（window.opacity）
# ============================================================================

winease_add_plugin(we_window_opacity
    CLASS_NAME WindowOpacityPlugin
    SOURCES
        window_opacity_plugin.h
        window_opacity_plugin.cpp
        window_opacity_plugin.json
)
