#pragma once

// ============================================================================
//  feature_smoke / system_group.h —— P1-D 系统组端到端用例
//            （P1-05 右键菜单 / P1-09 网页搜索 / P1-13 主题切换 / P1-14 壁纸切换）
//
//  这一组的共同点是**都写系统状态**（注册表 / 桌面壁纸），所以硬指标只有一条：
//      **停用功能 → 系统状态必须回到"启用之前"的样子**。
//  用例因此都长成同一个形状：
//      ① 读下原值（前置，读不到就明确失败——没有基准就谈不上"还原"）
//      ② 启用 → 触发功能 → 断言**系统层面的真实状态**变了
//      ③ 停用 → 再读一遍 → 断言与原值逐项一致
//
//  ⚠ 本组会**短暂改动真实系统**：注册表里会出现 WinEase.* 菜单项、
//    窗口主题会闪一下深/浅色、壁纸会临时换成自检造的纯色图。
//    用例结束都会还原；跑的时候看到屏幕闪一下属正常。
// ============================================================================

#include "test_support.h"

namespace FeatureSmoke {

/// 返回失败项数（0 表示全部通过）
int runSystemGroupTests(Reporter &reporter,
                        WinEase::PluginManager &manager,
                        StubServices &services,
                        ProbeWindow &probe);

} // namespace FeatureSmoke
